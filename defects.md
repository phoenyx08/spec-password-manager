# Defects observed in version 0.0.1

1. The agent creates files in the root of spec project. All the output is supposed to be placed in `yvopassman`
directory
2. `.deb` package is not created after the agent finished building
3. After the `.deb` package is generated by AI it is created in the parent directory of the spec project
